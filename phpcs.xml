<?xml version="1.0"?>
<ruleset name="ShipMonk SortedLinkedList">
  <description>Clean, minimal code style rules for ShipMonk SortedLinkedList</description>

  <!-- Targets -->
  <file>src</file>
  <file>tests</file>

  <!-- Exclude non-source -->
  <exclude-pattern>vendor/*</exclude-pattern>
  <exclude-pattern>bin/*</exclude-pattern>

  <!-- Base standard -->
  <rule ref="PSR12"/>

  <!-- Line length (reasonable limits) -->
  <rule ref="Generic.Files.LineLength">
    <properties>
      <property name="lineLimit" value="120"/>
      <property name="absoluteLineLimit" value="150"/>
      <property name="ignoreComments" value="true"/>
    </properties>
  </rule>

  <!-- Essential formatting rules -->
  <rule ref="Generic.Files.EndFileNewline"/>
  <rule ref="Generic.Files.LineEndings">
    <properties>
      <property name="eolChar" value="\n"/>
    </properties>
  </rule>
  <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>
  <rule ref="Squiz.WhiteSpace.OperatorSpacing"/>

  <!-- Relaxed documentation rules (optional) -->
  <rule ref="Squiz.Commenting.FileComment"><severity>0</severity></rule>
  <rule ref="Squiz.Commenting.ClassComment"><severity>0</severity></rule>
  <rule ref="Squiz.Commenting.FunctionComment"><severity>0</severity></rule>
</ruleset>